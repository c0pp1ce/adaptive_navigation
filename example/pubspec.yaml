name: adaptive_navigation_widget_example
description: A Flutter package example for adaptive_navigation_widget.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=3.1.0"

dependencies:
  flutter:
    sdk: flutter

  adaptive_navigation_widget:
    path: ../

  go_router: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
